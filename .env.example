# Docker Compose Environment Variables
#
# SECURITY WARNING: These are example values for local development only.
# NEVER use these values in production or commit actual secrets to version control.
#
# Generate strong credentials with:
#   POSTGRES_PASSWORD: openssl rand -base64 32 | tr -d '/+='
#   S3_ACCESS_KEY: openssl rand -base64 16 | tr -d '/+=' | cut -c1-16
#   S3_SECRET_KEY: openssl rand -base64 32 | tr -d '/+='
#   REDIS_PASSWORD: openssl rand -base64 32 | tr -d '/+='
#   MINIO_ENCRYPTION_KEY: python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
#   ENCRYPTION_MASTER_KEY: python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"

# Database Security
# Generate with: openssl rand -base64 32 | tr -d '/+='
# Minimum 32 characters required
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_RANDOM_32_CHARS

# Redis Security
# Generate with: openssl rand -base64 32 | tr -d '/+='
# Minimum 32 characters required
REDIS_PASSWORD=CHANGE_ME_GENERATE_RANDOM_32_CHARS

# MinIO/S3 Credentials
# Access Key: 16+ characters (username)
# Secret Key: 32+ characters (password)
# Generate access key: openssl rand -base64 16 | tr -d '/+=' | cut -c1-16
# Generate secret key: openssl rand -base64 32 | tr -d '/+='
S3_ACCESS_KEY=CHANGE_ME_16_CHARS
S3_SECRET_KEY=CHANGE_ME_GENERATE_RANDOM_32_CHARS

# MinIO Encryption (Required for HIPAA compliance §164.312(a)(2)(iv))
# Generate with: python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# Must be 32 bytes (256 bits) base64-encoded
MINIO_ENCRYPTION_KEY=CHANGE_ME_GENERATE_BASE64_32_BYTES

# Application Encryption (PHI encryption for database fields)
# Required for HIPAA compliance §164.312(a)(2)(iv) - Encryption at rest
# Used by backend API and arq-worker for encrypting/decrypting sensitive data (SOAP notes, client PII)
# Generate with: python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# Must be 32 bytes (256 bits) base64-encoded
ENCRYPTION_MASTER_KEY=CHANGE_ME_GENERATE_BASE64_32_BYTES


# PazPaz Google Calendar Integration ID
GOOGLE_CALENDAR_INTEGRATION_PROJECT_ID=pazpaz-calendar-integration
